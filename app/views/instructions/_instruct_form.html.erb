<%= form_for [@recipe, @instruction] do |f| %>

  <div class="error-messages">
    <% if @instruction.errors.any? %>
      <p><%= pluralize(@instruction.errors.count, "error(s)") %> prohibited this instruction from being saved:</p>
      <ul>
        <% @instruction.errors.full_messages.each do |m| %>
          <li><%= m %></li>
        <% end %>
      </ul>
    <% end %>
  </div>

  <div>
    <%= f.label :step %><br>
    <%= f.text_field :step %>
  </div><br>

  <div>
    <%= f.submit '+'%><br><br>
  </div>

<% end %>
