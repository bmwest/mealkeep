<% if current_user && @profile.user == current_user %>
  <div class="profile-header">
    <h2>A little bit about <%= current_user.first_name %>,</h2>
  </div>

  <div class="user-blurb">
    <p><%= @profile.about_me %></p>
  </div>
<% end %>
